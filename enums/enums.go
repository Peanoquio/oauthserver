package enums

// Platform is the enum for the different platforms that supports oauth
type Platform uint

const (
	// Google platform
	Google Platform = 1
	// Facebook platform
	Facebook Platform = 2
)
